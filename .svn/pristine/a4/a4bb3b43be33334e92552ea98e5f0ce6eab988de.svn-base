#!/bin/bash

NUM_PROBLEMS=100

minBits=8 # Exponent
maxBits=48

minN=20
maxN=100

for n in $(seq $minN 1 $maxN)
do
      echo "python ./script/RunExperimentsBest2.py -b $minBits -B $maxBits -n $n -x $NUM_PROBLEMS"
done


